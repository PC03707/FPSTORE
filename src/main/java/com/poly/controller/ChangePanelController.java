package com.poly.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import com.poly.dao.ChangePanelDAO;
import com.poly.entity.ChangePanel;

import java.util.List;

@RestController
@RequestMapping("/changepanels")
public class ChangePanelController {


//    @GetMapping("/{id}")
//    public ChangePanel getChangePanelById(@PathVariable int id) {
//        return changePanelService.getChangePanelById(id);
//    }

    // Các phương thức khác xử lý request
}
